import React from 'react'

export default function NewBatch() {

    const addNewBatch = () => {
      // show popup form
      // user adds batch details
      // submit -> make request to backend

      // retrieve response
      // if cool -> add batch to workflow

      // some details
      
    }


  return (
    <div>
    <button
      onClick={addNewBatch}
      type='submit'
      className='btn-class min-w-[100px] max-h-[35px]'
    >
      Add new
    </button>
    </div>
  )
}
